# Only needed for live hardware mode (python agent/agent.py live COM3).
# practice mode needs nothing beyond the Python standard library.
pyserial>=3.5

# Only needed for the live graph (python agent/graph.py ...)
matplotlib>=3.5
